Description: Fix for stupid iniparser packaging
 Ubuntu for some reason packages iniparser without any .pc file
 and with the header in a subdirectory (until plucky)
Author: Constantin Piber <cp.piber@gmail.com>

Index: source/CMakeLists.txt
===================================================================
--- source.orig/CMakeLists.txt
+++ source/CMakeLists.txt
@@ -37,7 +37,6 @@ pkg_check_modules(
   pango
   cairo
   pangocairo
-  iniparser
   hyprgraphics>=0.3.0
   aquamarine>=0.9.5
 )
@@ -77,7 +76,7 @@ target_include_directories(
   PRIVATE "./src" "./src/include" "./protocols" "${CMAKE_BINARY_DIR}")
 set_target_properties(hyprtoolkit PROPERTIES VERSION ${HYPRTOOLKIT_VERSION}
                                             SOVERSION 2)
-target_link_libraries(hyprtoolkit PUBLIC OpenGL::EGL OpenGL::OpenGL PkgConfig::deps)
+target_link_libraries(hyprtoolkit PUBLIC OpenGL::EGL OpenGL::OpenGL PkgConfig::deps iniparser)
 
 check_include_file("sys/timerfd.h" HAS_TIMERFD)
 pkg_check_modules(epoll IMPORTED_TARGET epoll-shim)
Index: source/src/system/Icons.cpp
===================================================================
--- source.orig/src/system/Icons.cpp
+++ source/src/system/Icons.cpp
@@ -12,7 +12,7 @@
 #include <hyprutils/utils/ScopeGuard.hpp>
 
 extern "C" {
-#include "iniparser.h"
+#include "iniparser/iniparser.h"
 }
 
 using namespace Hyprtoolkit;
@@ -201,4 +201,4 @@ SP<ISystemIconDescription> CSystemIconFa
         return makeShared<CSystemIconDescription>();
 
     return makeShared<CSystemIconDescription>(iconName);
-}
\ No newline at end of file
+}
